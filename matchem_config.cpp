#include "matchem_config.hpp"
#include "matchem_exception.hpp"

#include <sstream>

namespace matchem {

////////////////////////////////////////////////////////////////////////////////
MatchemConfig::MatchemConfig(
////////////////////////////////////////////////////////////////////////////////
  const SimulationType sim_type,
  const int num_runs,
  const int set_size) :
  m_sim_type(sim_type),
  m_num_runs(num_runs),
  m_set_size(set_size)
{}

////////////////////////////////////////////////////////////////////////////////
std::ostream& MatchemConfig::operator<<(std::ostream& out) const
////////////////////////////////////////////////////////////////////////////////
{
  out << "sim type: " << m_sim_type << "\n";
  out << "num runs: " << m_num_runs << "\n";
  out << "set size: " << m_set_size << "\n";

  return out;
}

////////////////////////////////////////////////////////////////////////////////
std::ostream& operator<<(std::ostream& out, const MatchemConfig& config)
////////////////////////////////////////////////////////////////////////////////
{
  return config.operator<<(out);
}

}
